# Voice Cloning App - UI (Vanilla JS) + Backend (Flask + Chatterbox API)

To view, run the following in the project root after moving landingpage.mp4 to ./static/:
```
pip install -r requirements.txt
flask run
```
